1.

Известно, что в списке `t` сначала идут числовые элементы, а затем строковые. Напишите функцию `split_list`, которая принимает на вход один список, делит этот список на два по типам данных, возвращает два списка.
Обязательно проверяйте, что ваша функция работает, можете подать для проверки этот список или любой другой:
```
t = [0, 9, 56, -34, 5, 'a', 'abc', 'def']
```

