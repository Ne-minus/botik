4.

Дан список элементов, содержащих `n` различных чисел из отрезка `[0, n]`. Напишите функцию `get_missing`, которая принимает на вход список и возращает недостающее число из этого списка.

Пример:
```
>>> get_missing([0, 2])
1
>>> get_missing([1, 2, 3])
0
>>> get_missing([0, 1])
2
```