# Задача №1

Для производного частотного словаря (словаря, в котором ключи -- это что угодно, а значения -- неотрицательные числа)
в котором частоты -- абсолютные,
переведите значения частот в относительные.

Например, `{'a': 2, 'b': 3}` должен превратиться в `{'a': 0.4, 'b': 0.6}`
